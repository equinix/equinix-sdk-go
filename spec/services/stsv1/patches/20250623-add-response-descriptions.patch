diff --git a/spec/services/stsv1/oas3.patched/openapi.yaml b/spec/services/stsv1/oas3.patched/openapi.yaml
index 5eef4d06..a3b89813 100644
--- a/spec/services/stsv1/oas3.patched/openapi.yaml
+++ b/spec/services/stsv1/oas3.patched/openapi.yaml
@@ -2,11 +2,20 @@ openapi: 3.1.0
 x-id:
   - reitit.openapi/default
 info:
-  title: EaaS Security Token Service
-  summary: >-
-    Exchange ID tokens for EaaS access tokens according to managed trust
+  title: Equinix Security Token Service
+  description: >-
+    Exchange ID tokens for Equinix access tokens according to managed trust
     relationships.
-  version: not specified
+  version: 1.0.0
+  termsOfService: 'https://www.equinix.com/about/legal/terms'
+  contact:
+    name: Equinix API Support
+    url: 'https://docs.equinix.com/api-support.htm'
+  license:
+    name: Equinix Inc
+    url: 'https://developer.equinix.com/agreement'
+servers:
+  - url: 'https://sts.eqix.equinix.com'
 tags:
   - name: use
     description: Operations for normal users of this service
@@ -257,20 +266,6 @@ components:
 security:
   - bearerAuth: []
 paths:
-  /.well-known/openid-configuration:
-    get:
-      '200':
-        content:
-          application/json:
-            schema:
-              type: object
-  /jwks:
-    get:
-      '200':
-        content:
-          application/json:
-            schema:
-              type: object
   /ops/createOidcProvider:
     post:
       summary: /ops/createOidcProvider
@@ -357,6 +352,9 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns the OIDC provider that was created. The `idpId` field is the
+            unique identifier for this provider.
           content:
             application/json:
               schema:
@@ -367,6 +365,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -393,6 +395,10 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns a list of OIDC providers that are trusted by the specified
+            project. The `idpId` field is the unique identifier for each
+            provider.
           content:
             application/json:
               schema:
@@ -405,6 +411,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -465,6 +475,10 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns an OIDC ID token that identifies the service in the context
+            of a specific project, and with one or more audiences as the
+            intended recipients. The ID token is valid for 5 minutes.
           content:
             application/json:
               schema:
@@ -475,6 +489,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -511,6 +529,11 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns a list of client registrations for the specified service.
+            The `list` field contains the client registrations, and the
+            `nextPageToken` field can be used to retrieve the next page of
+            results.
           content:
             application/json:
               schema:
@@ -531,6 +554,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -580,6 +607,10 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns the client registration that was created. The `clientId` is
+            the unique identifier for this client, and the `clientSecret` is the
+            secret that should be used to authenticate the client.
           content:
             application/json:
               schema:
@@ -602,6 +633,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -670,6 +705,9 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns the OIDC provider that was created. The `idpId` field is the
+            unique identifier for this provider.
           content:
             application/json:
               schema:
@@ -680,6 +718,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -709,6 +751,9 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns a null result indicating that the OIDC provider was
+            successfully deleted.
           content:
             application/json:
               schema:
@@ -719,6 +764,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -742,6 +791,10 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns a list of OIDC providers that are trusted by the root
+            project. The `idpId` field is the unique identifier for each
+            provider.
           content:
             application/json:
               schema:
@@ -754,6 +807,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -776,6 +833,10 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns a list of suspended OIDC providers that are defined for the
+            root project. The `idpId` field is the unique identifier for each
+            provider.
           content:
             application/json:
               schema:
@@ -788,6 +849,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -857,6 +922,9 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns the OIDC provider that was modified. The `idpId` field is
+            the unique identifier for this provider.
           content:
             application/json:
               schema:
@@ -867,6 +935,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -893,6 +965,10 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns a null result indicating that the OIDC provider was
+            successfully resumed. The `idpId` field is the unique identifier for
+            this provider.
           content:
             application/json:
               schema:
@@ -903,6 +979,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -931,6 +1011,10 @@ paths:
               additionalProperties: false
       responses:
         '200':
+          description: >-
+            Returns a null result indicating that the OIDC provider was
+            successfully suspended. The `idpId` field is the unique identifier
+            for this provider.
           content:
             application/json:
               schema:
@@ -941,6 +1025,10 @@ paths:
                 required:
                   - result
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
@@ -980,6 +1068,10 @@ paths:
                 - grant_type
       responses:
         '200':
+          description: >-
+            Returns an access token that can be used to access other Equinix
+            product APIs. The `access_token` field contains the token, and the
+            `token_type` field indicates the type of token returned.
           content:
             application/json:
               schema:
@@ -995,7 +1087,6 @@ paths:
                     type: string
                     enum:
                       - Bearer
-                      - DPoP
                   expires_in:
                     type: number
                 required:
@@ -1004,6 +1095,10 @@ paths:
                   - token_type
                   - expires_in
         '400':
+          description: >-
+            The request was invalid. This could be due to a missing required
+            parameter, an invalid value, or a conflict with an existing
+            resource.
           content:
             application/json:
               schema:
